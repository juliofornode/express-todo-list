extends layout

block content
  .home
    if (tasks.length === 0)
      | No tasks.
    each task, index in tasks
      .item
        div.num
          span=index+1
            |.&nbsp;
        div.name.completed-task
          span.name=task.name + ' (Completed at: ' + moment(task.completeTime).format("YYYY/MM/DD") + ')'

